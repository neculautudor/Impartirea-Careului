#include <iostream>
#include <graphics.h>
#include <winbgim.h>
#define WIDTH 1000
#define HEIGHT 1000
#define TITLE "Impartirea Careului"

int main() {
    // Declaratii variabile

    // Initializeaza fereastra
    initwindow(WIDTH, HEIGHT, TITLE);

  
    getch();
    closegraph();

    return 0;
}
